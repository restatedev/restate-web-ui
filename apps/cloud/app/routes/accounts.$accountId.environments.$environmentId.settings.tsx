import { settings } from '@restate/features/cloud/environment-route';

export const clientLoader = settings.clientLoader;
export const clientAction = settings.clientAction;
export default settings.Component;
