import type { PropsWithChildren } from 'react';

type DropdownTriggerProps = unknown;

export function DropdownTrigger({
  children,
}: PropsWithChildren<DropdownTriggerProps>) {
  return children;
}
