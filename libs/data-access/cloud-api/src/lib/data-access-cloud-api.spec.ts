import { dataAccessCloudApi } from './data-access-cloud-api';

describe('dataAccessCloudApi', () => {
  it('should work', () => {
    expect(dataAccessCloudApi()).toEqual('data-access-cloud-api');
  });
});
